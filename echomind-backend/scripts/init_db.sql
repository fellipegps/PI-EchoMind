-- ─── EchoMind – Inicialização do Banco ────────────────────────────────────────
-- Executado automaticamente na primeira inicialização do volume.

CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
